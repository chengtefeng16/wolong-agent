# 卧龙 Agent 进度总结 2026-04-04

## 一句话总结

**WhatsApp 全链路今日完整打通**：真实用户消息从手机发出，经 ngrok 永久隧道进入系统，自动分析存档，H5 控制台实时展示。

---

## 今日工作回顾

### 上午：Cloudflare Named Tunnel 调试（最终放弃）

**目标**：为 webhook.zcarmy.com 配置 Cloudflare Named Tunnel，获得固定公网 URL。

**过程**：
1. 在阿里云 DNS 添加 CNAME 记录：`webhook.zcarmy.com → <tunnel-id>.cfargotunnel.com`
2. 通过 `curl --noproxy '*'` 绕过系统代理获取 cert.pem（系统代理 127.0.0.1:40009 阻断了 cloudflared 登录流程）
3. DNS 解析失败：`nslookup webhook.zcarmy.com` 无法解析

**根因**：zcarmy.com 的 Nameserver 是阿里云（hichina），不是 Cloudflare。cfargotunnel.com 的 CNAME 只在 Cloudflare 的 DNS 体系内可见（需要橙色云图标 Proxy 才能解析），通过阿里云 NS 对外不可见。

**结论**：放弃 Cloudflare Named Tunnel，改用 ngrok 免费永久域名方案。

---

### 下午：ngrok 永久隧道搭建

**方案**：ngrok 免费账户提供一个固定永久子域名。

**步骤**：
1. 注册 ngrok 账号，获取 authtoken
2. 申请永久域名：`porter-unsimultaneous-nonopprobriously.ngrok-free.dev`
3. 启动命令（关键：必须 unset 系统代理环境变量）：
   ```bash
   env -u http_proxy -u https_proxy -u HTTP_PROXY -u HTTPS_PROXY -u ALL_PROXY -u all_proxy \
     ngrok http 8765 --url=porter-unsimultaneous-nonopprobriously.ngrok-free.dev
   ```
4. 在 Meta Developer Console 配置 Webhook URL 并完成 Token 验证

**结果**：✅ Webhook 验证通过，测试事件可正常推送。

---

### 下午：全链路调试与问题定位

**现象**：Meta 控制台"发送到服务器"按钮可触发 Webhook，但从真实手机发消息没有任何反应。

**排查过程**：

#### 问题 1：H5 不显示消息
- 发现 `rebuild_dashboard()` 从未被调用
- api_server.py 的 webhook handler 只调用了 `sync_once()`（文件拷贝），但 H5 JSON 从未被重建
- **修复**：在 webhook handler 中收到消息后先调用 `rebuild_dashboard()`，再调用 `sync_once()`

#### 问题 2：H5 聊天内容为空
- H5 客户卡片显示，但点开"聊天内容"为空
- 原因：conversation_index.json 存储电话号为 `+8613122101699`（带 +），但文件名是 `8613122101699.json`（不带 +）
- **修复**：`runtime_whatsapp_h5_sync_v1.py` 中查不到文件时自动 `lstrip('+')`

#### 问题 3：真实消息完全不触发 Webhook（最根本问题）
- Meta 控制台测试可以，但真实手机消息无响应
- 检查 WABA 订阅状态：
  ```
  GET https://graph.facebook.com/v20.0/2817262535309287/subscribed_apps
  → {"data": []}  ← 空！从未订阅！
  ```
- **根因**：Meta 的 WhatsApp Business Account (WABA) 需要显式订阅才能把消息路由到 App 的 Webhook。测试按钮直接 POST 到 URL，绕过了这个路由机制。
- **修复**：
  ```bash
  curl -X POST \
    "https://graph.facebook.com/v20.0/2817262535309287/subscribed_apps" \
    -H "Authorization: Bearer <SYSTEM_USER_TOKEN>"
  → {"success": true}
  ```
- **结果**：执行后立即生效，真实手机消息开始触发 Webhook ✅

---

### 验证成功

**15:29:37** — 坤越通国际贸易（+86 188 0180 1477）发来消息"我想要100台英菲尼迪加奔驰"

- ✅ ngrok 接收到 POST 请求
- ✅ api_server.py webhook handler 处理入库
- ✅ `rebuild_dashboard()` 重建 H5 JSON
- ✅ `sync_once()` 拷贝到 wolong_h5_console/public/
- ✅ H5 控制台（8秒轮询）显示新客户卡片
- ✅ 聊天内容显示正确消息

**全链路打通！**

---

## 今日修改的文件

| 文件 | 修改内容 |
|------|----------|
| `scripts/api_server.py` | webhook handler 增加 `rebuild_dashboard()` 调用 |
| `qianqiu_os/services/runtime_whatsapp_h5_sync_v1.py` | 电话号码 `lstrip('+')` 路径兼容 |
| `scripts/start_wolong.sh` | 替换 Cloudflare Tunnel → ngrok 永久隧道；增加 WABA 订阅激活步骤 |
| `PROJECT_MEMO.md` | 新增 2026-04-04 里程碑 |

---

## 当前系统架构（2026-04-04）

```
真实 WhatsApp 用户
       ↓ 发消息
Meta WhatsApp Cloud API
       ↓ HTTP POST（需要 WABA 已 subscribed_apps）
ngrok 永久隧道
porter-unsimultaneous-nonopprobriously.ngrok-free.dev/webhook
       ↓ 转发到本地
api_server.py :8765/webhook
       ↓ 解析 & 存档
qianqiu_os/runtime_sessions/whatsapp/
  ├── conversation_index.json
  └── conversations/<phone>.json
       ↓ rebuild_dashboard()
qianqiu_os/runtime_views/h5_dashboard_whatsapp.json
       ↓ sync_once()
wolong_h5_console/public/runtime/views/h5_dashboard_whatsapp.json
       ↓ 8秒轮询
卧龙 H5 控制台 :5173
```

---

## 关键经验沉淀

1. **Meta WABA 订阅是必须的**：`subscribed_apps` 为空时，真实消息静默丢失，测试按钮不受影响，极易误判为代码问题。每次重置 App 或新建 WABA 后必须重新订阅。

2. **start_wolong.sh 已加入 WABA 订阅激活**：每次启动系统会自动调用 `POST /subscribed_apps`，确保不因重启失效。

3. **ngrok 与系统代理冲突**：MacOS 系统级代理（127.0.0.1:40009）会阻断 ngrok 连接（ERR_NGROK_9009）。解决方法：用 `env -u ...` unset 代理环境变量启动 ngrok。

4. **H5 同步是两步**：重建（`rebuild_dashboard`）+ 拷贝（`sync_once`）必须都调用，只调用任意一步均不完整。

5. **Cloudflare Tunnel 依赖 NS**：在非 Cloudflare NS 的域名下无法使用 Named Tunnel（cfargotunnel.com CNAME 在公网不可解析）。改用 ngrok / frp / pagekite 等方案更简单。

---

## 下一步计划

- [ ] 接入 Gemini AI 对新入库消息自动分类（bucket 字段）
- [ ] 接入 Gemini AI 自动提取 wants/conditions/destination
- [ ] 开启 auto_classify=true 小范围测试
- [ ] CRM 状态（crm_status）自动更新
- [ ] 考虑 auto_reply 测试（需先完成分类准确率验证）
