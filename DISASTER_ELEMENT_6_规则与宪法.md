# 灾难备份第六要素：规则与宪法固化
> 版本：V1 | 日期：2026-03-22 | 项目：AgentOS / 卧龙Agent

---

## 一、本文档用途

本文档是 AgentOS / 卧龙Agent 的**最高治理宪法**。
所有开发、迁移、新增功能、重构决策，均以本文档为最终判断依据。

原始文档来源：
- 《统一 Agent 协议 V1（边界优先版）》260320
- 《AgentOS / 卧龙Agent 旧模块映射与兼容迁移清单 V1》260320
- 《AgentOS / 卧龙Agent 边界表与五张关键映射表 V1》260320

---

## 二、最高原则（五条宪法级规则）

> 这五条规则高于一切，任何开发动作违反其中任何一条，必须停下来重新判断。

1. **先文档，先备份，先兼容，再迁移**
2. **旧模块先判断归属，再决定是否改造；能包裹先包裹，能映射先映射**
3. **新增功能优先走 AgentOS 新主干，不再继续长到主干外面**
4. **卧龙先收口，不先拆空**（卧龙是第一优先级业务Agent，不因公共化而削弱落地速度）
5. **不默认删旧功能，不为了整洁破坏已跑通链路**

---

## 三、总边界表（AgentOS / 卧龙 最终归属）

| 边界对象 | 归属 | 处理原则 |
|----------|------|----------|
| 多源输入接入 | AgentOS 公共能力 | 统一接入壳层 |
| 路由分发 | AgentOS 公共能力 | 统一路由中心 |
| task/session/trace 生命周期 | AgentOS 公共能力 | 任务壳标准化 |
| 上下文 / 状态 / 记忆总线 | AgentOS 公共能力 | 统一读写接口 |
| 工具网关 | AgentOS 公共能力 | 所有工具统一出口 |
| 治理 / 审计 / 学习 / 自愈 / 观测 | AgentOS 跨层能力 | 逐步标准化 |
| 输出与交付主框架 | AgentOS 公共能力 | 统一结果交付链 |
| H5 框架与公共展示壳 | AgentOS 公共能力 | 统一结果卡消费 |
| 业务 prompt | 卧龙 私有能力 | 继续独立演进 |
| 二手车国际贸易语义理解 | 卧龙 私有能力 | 对外只暴露标准结果 |
| 意图识别细则 | 卧龙 私有能力 | 统一意图标签输出 |
| 填槽策略 | 卧龙 私有能力 | 统一槽位结果表达 |
| 快法规使用策略 | 卧龙（调用公共法规能力）| 向标准法规引擎收口 |
| 深法规触发策略 | 卧龙（异步旁轨）| 标准 followup task |
| 客户评分逻辑 | 卧龙 私有能力 | 标准异步任务挂接 |
| CRM 业务沉淀规则 | 卧龙 私有能力 | 标准输出/归档挂接 |
| 法规引擎输入输出协议 | AgentOS 公共可调用能力 | 必须标准化 |
| 日报/周报/checkpoint 导出链 | AgentOS 输出与交付 | 统一交付框架 |

---

## 四、公共状态标准集合（V1 正式口径）

所有模块对外统一使用以下状态值，不得自造：

| 公共状态 | 含义 |
|----------|------|
| `created` | 刚创建，尚未进入路由 |
| `routed` | 已接入并进入分发/排队 |
| `running` | 主执行中 |
| `waiting_followup` | 主结果已出，异步旁路仍在排队 |
| `degraded` | 降级路径，非完全失败 |
| `completed` | 主任务完成 |
| `failed` | 执行失败或中断 |
| `archived` | 已完成交付并进入归档 |

---

## 五、标准 followup task 类型（V1 正式口径）

卧龙当前必须固定的四类 followup task：

| followup_type | 归属 | 结果回流位置 |
|---------------|------|-------------|
| `deep_regulation_check` | 卧龙异步旁轨 | 审计 / H5 / CRM / 补充回复 |
| `customer_scoring` | 卧龙异步旁轨 | H5 / CRM / 审计 |
| `crm_sync` | 卧龙异步旁轨 | CRM / 审计 |
| `audit_enrichment` | 审计跨层能力 | 审计 / 周报 |

每个 followup task 必须包含字段：
`followup_task_id` / `parent_task_id` / `agent_id` / `followup_type` / `payload` / `priority` / `expected_deadline` / `status`

---

## 六、工具调用五条铁律

1. 工具注册归 AgentOS 工具网关
2. 工具调用出口归 AgentOS 工具网关
3. 业务 Agent 只能通过授权策略使用工具，不应默认裸调
4. 高风险工具必须受治理控制
5. 所有对外写入、发送、落库类工具，必须纳入审计留痕

---

## 七、标准交付对象类型（V1 正式口径）

H5、日报、周报、导出文档必须优先消费以下标准对象：

| 交付对象类型 | 面向谁 | 典型内容 |
|-------------|--------|----------|
| `user_text_reply` | 用户 | 即时文本回复 |
| `markdown_report` | 用户/内部 | Markdown 成果 |
| `pdf_report` | 用户/内部 | PDF 成果 |
| `regulation_card` | 用户/H5/内部 | 法规摘要卡 |
| `customer_profile_card` | H5/内部 | 客户画像摘要卡 |
| `task_result_card` | H5/内部 | 任务结果卡 |
| `audit_summary` | 内部 | 审计摘要 |
| `checkpoint_record` | 内部 | 阶段性检查点 |
| `daily_digest` | 内部 | 每日摘要 |
| `weekly_digest` | 内部 | 周报摘要 |
| `crm_payload` | 内部/系统 | CRM 写入对象 |
| `export_record` | 内部 | 导出记录 |

---

## 八、V1 阶段明确禁止的七件事

1. ❌ 一上来就按新结构大规模移动代码目录
2. ❌ 一上来就删除旧逻辑（旧逻辑承载真实跑通价值）
3. ❌ 一上来要求所有字段/状态/工具一次性全部标准化
4. ❌ 过早抽空卧龙的业务能力（卧龙是第一优先级业务Agent）
5. ❌ 把已经稳定运行的交付链硬拆成过细模块
6. ❌ 让 H5 直接吃散乱私有字段，不经过适配壳
7. ❌ 继续新增新的"野入口"而不经过统一接入壳层

---

## 九、V1 阶段推荐的迁移顺序

**第一步** — 冻结三份正式文档（已完成 ✅）
**第二步** — 补五张关键表（边界表、状态映射表、工具权限表、followup task表、交付对象表）（已完成 ✅）
**第三步** — 先做四个壳层（输入壳、输出壳、状态映射壳、工具网关包裹壳）
**第四步** — 卧龙先收口，不先大拆
**第五步** — 新增功能按新主干走，通过新增功能带动旧系统逐步收口

---

## 十、遇到模块归属判断时的四问法

碰到任何一个模块，先问这四个问题：

1. 这是多个 Agent 都会用的吗？→ 是，大概率归 AgentOS
2. 这是卧龙特有业务理解或业务规则吗？→ 是，大概率归卧龙
3. 这是公共底座该提供的读写/治理/交付/工具控制吗？→ 是，大概率归 AgentOS
4. 这是卧龙当前特有实现，但未来可能抽成公共能力吗？→ 当前先留兼容区，方向上往 AgentOS 收口

---

## 十一、正式执行口径（最终生效版本）

> 自本文档确认后，所有后续迁移、开发、重构动作，统一采用以下口径：

**先查表，再改代码。先兼容挂接，再逐步收口。新增功能优先按表落位。不默认删旧功能。**

具体判断顺序固定为：
1. 先判断归属（查总边界表）
2. 再判断状态表达（查状态映射表）
3. 再判断工具权限（查工具权限归属表）
4. 再判断是否旁路（查 followup task 类型表）
5. 再判断交付对象（查交付对象类型表）
6. 最后决定当前阶段动作（保留 / 包裹 / 映射 / 标准化）

---

*本文档为灾难备份六要素第六要素，是本项目最高治理宪法，不得随意修改。*
*如需修改，须同步更新原始三份协议文档，并在 Git 中留下完整变更记录。*
